version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries"
    schema: "db/migrations"
    gen:
      go:
        package: "queries"
        out: "internal/queries/"
        sql_package: "pgx/v4"
        overrides:
          - db_type: "citext"
            go_type: "string"