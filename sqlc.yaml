version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/movies.sql"
    schema: "sql/movies.sql"
    gen:
      go:
        package: "movies"
        out: "internal/movies"
        sql_package: "pgx/v5"
        emit_db_tags: true
        emit_json_tags: true
