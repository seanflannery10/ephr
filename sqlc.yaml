version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries"
    schema: "db/migrations"
    gen:
      go:
        package: "db"
        out: "internal/db/"
        sql_package: "pgx/v4"
        emit_db_tags: true
        emit_json_tags: true